management:
  endpoint:
    health:
      show-details: always

  endpoints:
    web:
      exposure:
        include: refresh
spring:
  application:
    name: user-service

  cloud:
    config:
      enabled: true
      
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
              
        
     
     
